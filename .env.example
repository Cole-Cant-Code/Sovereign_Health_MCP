# CIP Personal Health — Environment Configuration
# Copy this file to .env and fill in your values.

# --- Server ---
CIP_HOST=127.0.0.1
CIP_PORT=8001
CIP_LOG_LEVEL=info

# --- LLM Provider ---
# Options: "anthropic", "openai", "mock"
LLM_PROVIDER=mock

# Anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# --- Mantic (cip-mantic-core MCP service) ---
MANTIC_CORE_URL=http://127.0.0.1:8002/mcp

# --- Storage (health data bank) ---
DB_PATH=~/.cip/health.db

# --- Connectors ---
# Path to Apple Health export.xml (iOS → Health → Share → Export Health Data)
APPLE_HEALTH_EXPORT_PATH=

# --- Security ---
# Fernet key for data at rest encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# REQUIRED when DB_PATH is set — server refuses to start without it.
ENCRYPTION_KEY=
